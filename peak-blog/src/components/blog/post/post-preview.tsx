import React from "react";
import {PeakPost} from "component-library";

export const PostPreview = (props: { post: PeakPost }) => {
    const { post } = props

    return (
        <div>
            <div>

            </div>
        </div>
    )
}